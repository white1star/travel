# Stitch 设计参考与静态检查

项目：旅行规划网站设计（4397958274684739075）。

## 收到的原始导出

- `desktop-home.html`：桌面首页，画面 ID `9f772cfee1ad4f4aa185d23ffe2a8369`。
- `mobile-home.html`：移动首页，画面 ID `6cac18eb2e3f4b01af6668a0ba459d2f`。
- `card-interaction-spec.html`：薄荷版卡片交互规范，画面 ID `2ed2c516b2074f249fa8a39029605330`。

桌面首页未修改。移动首页与卡片规范已修复下述两处错误，原始导出分别保存在 `originals/mobile-home.html` 和 `originals/card-interaction-spec.html`。未修改正式网站应用，未部署。

## 动效确实存在于代码中

- 桌面首页入口卡片：悬停上浮 8px，阴影加深，顶部色线展开，图标背景改变。
- 移动首页入口卡片：悬停上浮 4px，按下缩放到 0.98。
- 规范页信息卡片：悬停上浮 8px，顶部色线通过 scaleX 展开。
- 规范页图文卡片：悬停上浮 5px，内部图片放大至 1.05 倍。
- 规范页选中状态：回落到 0px，显示深绿边框、浅薄荷背景和勾选标记。
- 规范页卡片过渡为 300ms，图片为 450ms，曲线为 cubic-bezier(0.16, 1, 0.3, 1)。

## 静态检查结果与接入前问题

最初使用 Node 的 vm.Script 进行语法解析定位错误。修复后另外使用已有 jsdom 环境执行真实内联脚本与点击处理，外部脚本和网络资源不参与测试。

- 桌面首页两个内联脚本：语法通过。
- 规范页两个内联脚本：语法通过。
- 移动首页配置脚本：已补齐 fontSize 中 code 属性的引号，配置可以执行，薄荷配色与字号配置正常读取。
- 规范页 setViewMode：已改用始终存在的 `.info-card, .trip-card` 查询目标，连续三轮触控与桌面切换后，十二张卡片的悬停类名都能恢复。
- 规范页多数选择项是带 onclick 的 div，未提供键盘选择语义；没有减少动态效果的适配。
- 首页导航多为 href="#" 和设计工具的 data-path，尚未连接网站路由；桌面主要按钮只滚动并聚焦输入框。
- 桌面地图是静态 SVG 示意，日期切换和地点联动未接入真实逻辑。
- 首页包含固定关西样例及精密测绘、误差和离线能力等原型文案，不能直接当作已实现的产品能力发布。

已修复移动配置和卡片模式切换两处错误，其余接入问题未实现。

五项回归测试全部通过：移动首页配置执行、桌面首页配置执行、悬停恢复、节奏单选跨模式保持、兴趣多选计数增减。测试先在原始导出上复现两项失败，再分别修复到全部通过。

配色、卡片 CSS 动效、页面内容与布局保持不变。本地 Chromium 实测：触控模式切回桌面模式后，十二张卡片恢复悬停类名，首张信息卡片悬停的计算位移为 -8px。移动首页在 390×844 视口下配置正常执行，页面宽度为 390px，没有横向溢出。移动首页等待完整 load 时超时，远程资源未完成加载；以上交互与配置检查不代表完整图片与视觉验收。控制台除 Tailwind CDN 的生产使用警告、预览站缺失 favicon 的 404 外，没有发现脚本执行错误。

## 图片与其他依赖

`manifest.json` 保存三个画面的对应关系，以及 HTML 中五个唯一图片地址和所属页面。

图片均从用户提供代码中的原始 Google 地址直接尝试下载，没有经过第三方代理。普通连接和获批连接都在 12 秒后超时，当前没有成功下载图片。原始 HTML 保留远程链接。

附件没有提供 Stitch 的原始画面截图，因此没有将页面内图片或本地渲染截图冒充画面截图。

HTML 还依赖远程 Tailwind CDN、Google 字体；正式接入时应改为项目组件和可控资源。
